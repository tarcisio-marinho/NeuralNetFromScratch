#include "neuralnet.h"
#include "matrix.h"
#include<vector>
#include<iostream>

int main(int argc, char *argv[]){

    Matrix *m = new Matrix(4, 2);
    
    
    // float (*foo)(float);
    // foo = &pi;

    return 0;
}